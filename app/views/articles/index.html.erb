<div class="container text-center">

  <% @articles.each do |article| %>
    <h2><%= article.title %></h2>
    <p><%= article.text %></p>
    <%= link_to 'Show', article_path(article) %>
  <% end %>
</div>
