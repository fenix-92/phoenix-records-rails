<style type="text/css">
  .text-warning{
    color: #895392!important;
  }
  td {
    display: table-cell!important;
    vertical-align: inherit!important;
  }
</style>
<div class="container">
  <div class="row">
    <div class="col-md-6">
          <div class="card card-stats">
            <div class="card-body table-responsive">
              <center><h3>Countries</h3></center>
              <table class="table table-hover">
                <thead class="text-warning">
                  <th>Name</th>
                  <th>Shortname</th>
                  <th>Flag</th>
                  <th>Actions</th>
                </thead>
                <tbody>
                  <% @country.each do |c| %>
                  <tr>
                    <td><%= c.name %></td>
                    <td><%= c.country_code %></td>
                    <td>
                    <% if c.image_flag.present? %>
                      <%= image_tag c.image_flag.admin.url, class: "img-fluid", size: '65x65' %>
                    <% else %>

                    <% end %>
                    </td>
                    <td>
                      <%= link_to 'Edit', edit_country_path(c) %> ·
                      <%= link_to "Delete", country_path(c), method: :delete %>
                    </td>
                  </tr>
                  <% end %>
                </tbody>
              </table>
          </div>
        </div>
    </div>
    <div class="col-md-6">
      <div class="card card-stats">
        <div class="card-body table-responsive">
          <center><h3>Actions</h3></center>
          <center><%= button_to 'New',{:controller => "countries", :action => "new"} , {:method=>:get, class: 'btn btn-primary'} %></center>
        </div>
      </div>
    </div>
  </div>
</div>
